# Archive

Retired material kept for historical reference only. **Don't use anything in this folder** — don't activate it as rules, don't copy it into new work, and don't treat it as current convention.

- `context-templates/` — old convention templates (backend, React + Vite, JS/TS). Superseded by project rules generated per-project in `.claude/rules/` (see `/atlas:get-to-know`).
